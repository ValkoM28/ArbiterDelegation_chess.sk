//just for the tailwind intellisense

/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./web/**/*.html" // scan all HTML files for class names
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }